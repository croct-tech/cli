// @ts-check

/** @type {import('next').NextConfig} */
const nextConfig = {
    0: 'unrelatedProp',
    i18n: {
        0: 'unrelatedProp',
        locales: ['en', 'pt'],
        defaultLocale: 'en',
    },
}

module.exports = nextConfig
